<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Change Password</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
  body {
    background: url("/Images/background.jpg") no-repeat center bottom fixed;
    background-size: cover;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Segoe UI', sans-serif;
  }
    .card {
      background: #1a1a40;
      padding: 2rem;
      border-radius: 1rem;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    }
    .form-control:focus {
      box-shadow: 0 0 5px #6b73ff;
      border-color: #6b73ff;
    }
    .input-group-text {
      cursor: pointer;
      background: #6b73ff;
      color: white;
      border: none;
      border-radius: 0 0.375rem 0.375rem 0;
    }
    .alert-info {
      background-color: #4c7af4 !important;
      color: #fff !important;
    }
  </style>
</head>
<body>
  <div class="card shadow">
    <h2 class="text-center mb-4">Change Password</h2>

    <% if (message) { %>
      <div class="alert alert-info text-center"><%= message %></div>
    <% } %>

    <form method="POST" action="/change-password" novalidate>
      <div class="mb-3">
        <label for="oldPassword" class="form-label">Old Password</label>
        <div class="input-group">
          <input
            type="password"
            id="oldPassword"
            name="oldPassword"
            class="form-control"
            placeholder="Enter your old password"
            required
            minlength="6"
          />
          <span class="input-group-text" onclick="toggleVisibility('oldPassword')">
            👁️
          </span>
        </div>
      </div>

      <div class="mb-4">
        <label for="newPassword" class="form-label">New Password</label>
        <div class="input-group">
          <input
            type="password"
            id="newPassword"
            name="newPassword"
            class="form-control"
            placeholder="Enter your new password"
            required
            minlength="6"
          />
          <span class="input-group-text" onclick="toggleVisibility('newPassword')">
            👁️
          </span>
        </div>
      </div>

      <button type="submit" class="btn btn-primary w-100 mb-3">Update Password</button>
      <a href="/dashboard" class="btn btn-outline-light w-100">Back to Dashboard</a>
    </form>
  </div>

  <script>
    function toggleVisibility(id) {
      const input = document.getElementById(id);
      if (input.type === "password") {
        input.type = "text";
      } else {
        input.type = "password";
      }
    }
  </script>
</body>
</html>
